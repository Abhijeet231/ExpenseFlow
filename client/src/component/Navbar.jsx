import React from 'react'

const Navbar = () => {
  return (
    <div>Navbar</div>
  )
}

export default Navbar


// so if user is loggedin then only show navbar - if user role is user - then show user name with a role badge . and logout button and at the left show brand name , and if role is namager then also show name and role and logout button right .  